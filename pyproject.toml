[tool.poetry]
name = "pipexec"
version = "0.1.0"
description = "Test pip packages quickly"
authors = ["Amos Omondi"]
license = "MIT"
readme = 'README.md'
repository = "https://github.com/amos-o/pipexec"

[tool.poetry.dependencies]
python = "~2.7 || ^3.4"
cleo = "^0.6.8"

[tool.poetry.dev-dependencies]
pylint = "^1.9"
